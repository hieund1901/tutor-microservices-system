spring:
  application:
    name: payment-service
  profiles:
    active: payment_service
  cloud:
    config:
      name: payment-service,config-client,config-security
      username: sonld36
      password: ${PASSWORD_CONFIG_SERVER}