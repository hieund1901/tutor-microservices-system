spring:
  application:
    name: signal-service
  profiles:
    active: signal_service
  cloud:
    config:
      name: config-client, signal-service, config-redis, config-security
      username: sonld36
      password: ${PASSWORD_CONFIG_SERVER}