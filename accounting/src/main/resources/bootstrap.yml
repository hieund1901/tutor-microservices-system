spring:
  application:
    name: accounting-service
  profiles:
    active: accounting
  cloud:
    config:
      name: accounting-service,config-client, config-security, config-kafka, config-minio
      username: sonld36
      password: ${PASSWORD_CONFIG_SERVER}