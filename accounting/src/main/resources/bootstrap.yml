spring:
  application:
    name: accounting-service
  profiles:
    active: accounting
  cloud:
    config:
      name: accounting-service,config-client, config-security
      username: sonld36
      password: ${PASSWORD_CONFIG_SERVER}