spring:
  application:
    name: course-service
  profiles:
    active: course_service
  cloud:
    config:
      name: course-service,config-client, config-security
      username: sonld36
      password: ${PASSWORD_CONFIG_SERVER}